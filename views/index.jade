extends layout
block headExtra
  script(src='/js/index.js')
  script.
    $(function(){
      console.log(parseInt($('#mob-wrapper').css('width'))-30);
      $('svg').attr('width', parseInt($('#mob-wrapper svg').parent().css('width'))-30);
      var attendStats = JSON.parse('!{params.attendStats}');
      makeDonutChart('', attendStats.datas);
    })
block content
  .container-fluid
    // Page Heading
    .row
      .col-lg-12
        h1.page-header
          | 출석 통계
          p
            small Statistics Overview
    // /.row
    .row
      .col-lg-12
        svg(width="700", height="500")
  // /.container-fluid
